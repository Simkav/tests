 ### Выведите список всех файлов в папке html
 cd /var/www/html #переход в папку html
 ls # вывод файлов
 ### Найдите в файле access.log все упоминания строки "404"
 cat /var/logs/acces.log | grep 404 # считываем файл access.log, перенаправляем вывод с помощью папйпа в grep и передаем в grep строку 404
 ### На основании файла 404.html сделайте файл 403.html заменив в его содержимом все упоминания 404 на 403
 cd /var/www/html # переход в папку html
 cp 404.html 403.html # копирование файла с переименованием в 403
 sed -r 's/404/403/g' -i 403.html # sed-редактор текста -r опция для подержки регулярок 's/404/403/g' - регулярка 404 заменить на 403 /g -означает что будет заменены все найденые вхождения, а не только первое -i опция для сохранения в рабочем файле
 ###Модифицируйте права доступа и владельца файла index.html следующим образом: Текущий пользователь должен иметь права на чтение/запись Группа www-data на чтение Все остальные не имеют доступа к файлу
 cd /var/www/html #переход в папку html
 sudo chown ${USER}:www-data index.html # chown для изменения владельца и группы файла
 sudo chmod 640 index.html # chmod для изменения прав (6-чтение и запись для владельца файла, 4-чтение для группы www-data,0-все остальные не имеют прав) 
 ### Сделайте символическую ссылку на файл access.log в папке html.
 cd /var/www/html #переход в папку html
 ln -s ../logs/access.log accesslink # ln для создания ссылка опция -s для указания что ссылка символическая, путь к файлу на который ссылаемся, путь к ссылке
 ### Выведите на экран все файлы в папке var
 ls -Ra /var # ls для вывода файлов, опция -а для показа скрытых файлов, -R рекурсивное отображение всех файлов
 ### Создайте новый файл 503.html в папке html с следующим содержимым
 printf "<html>\n    <head>\n        <title>503</title>\n    </head>\n</html>\n" > 503.html
 # printf для вывода в консоль, \n -символ перенос строк, > для записи в файл 503.html
